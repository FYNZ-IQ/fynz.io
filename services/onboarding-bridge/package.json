{
  "name": "fynz-onboarding-bridge",
  "version": "1.0.0",
  "description": "Webhook service that syncs GHL onboarding wizard answers into newly created SaaS sub-accounts (custom values + business profile).",
  "main": "server.js",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check sync.js && node --check ghl.js && node --check config.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
